import { OpenDatePickerDirective } from './open-date-picker.directive';

describe('OpenDatePickerDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenDatePickerDirective();
    expect(directive).toBeTruthy();
  });
});
